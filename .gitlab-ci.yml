stages:
  - build
  - deploy

build_job:
  stage: build
  image: docker:latest
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA .
    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA

deploy_job:
  stage: deploy
  image:
    name: bitnami/kubectl:latest # образ с  kubectl
    entrypoint: [""] # сбросить стандартную настройку образа, оставить просто bash 
       
  script:
    - export KUBECONFIG=$KUBECONFIG_CONTENT # где kubectl взять ключи и адреса для подключения к кластеру. GitLab (File type).
    - sed -i "s|127.0.0.1|$NODE_IP|g" $KUBECONFIG # переменная ip
    - sed -i "s|__IMAGE_NAME__|$CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA|g" k8s-deploy.yaml    
    
    - kubectl apply -f k8s-deploy.yaml --insecure-skip-tls-verify
    - kubectl apply -f ingress.yaml --insecure-skip-tls-verify
    
    - kubectl rollout status deployment/fastapi-app
